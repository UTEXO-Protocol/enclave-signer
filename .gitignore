/target/
# `parent/` is its own workspace (see the root Cargo.toml), so the anchored
# rule above does not cover its build dir.
/parent/target/
.claude/
.idea/
/docs/raw

# Local build artifacts (EIF, checksums, PCR measurements) — never committed,
# published to S3 instead.
/.artifacts/
