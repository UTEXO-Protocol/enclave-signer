/target/
# The parent is a separate cargo project (excluded from the workspace) and is
# built from its own directory, so its target/ needs its own entry.
/parent/target/
.claude/
.idea/
/docs/raw

# Local build artifacts (EIF, checksums, PCR measurements) — never committed,
# published to S3 instead.
/.artifacts/
