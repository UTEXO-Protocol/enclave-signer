name: "CD Dev"

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  cd:
    uses: ./.github/workflows/cd-reusable.yml
    with:
      stage: dev
      force_build: ${{ github.event_name == 'workflow_dispatch' }}
    secrets: inherit
