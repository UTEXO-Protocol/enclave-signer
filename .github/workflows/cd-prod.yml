name: "CD Prod"

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  cd:
    uses: ./.github/workflows/cd-reusable.yml
    with:
      stage: prod
      force_build: false
    secrets: inherit
